package dk.kvalitetsit.itukt.common.repository.core;

/**
 * This is just a non-sealed entrypoint for the InternalState which exposes it for extension
 * @param <T>
 */
public non-sealed interface State<T> extends InternalState<T> {

}
